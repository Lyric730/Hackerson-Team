# Hackerson 黑客松统一 Context

更新时间：2026-06-06
团队规模：3 人
赛道：抖音 AI 创变者计划 2026 黑客松 / 赛道一 / 互动空间

## 0. 来源

本文件由以下材料整理：

- `F:/WeChat Files/wxid_t4b5kciiz2vj22/FileStorage/File/2026-06/抖音互动空间Demo开发规范-黑客松版.md`
- `F:/WeChat Files/wxid_t4b5kciiz2vj22/FileStorage/File/2026-06/互动空间创作.md`
- `F:/WeChat Files/wxid_t4b5kciiz2vj22/FileStorage/Temp/49843d29af21f5389144c2a2a5eaa51.jpg`

## 1. 一句话目标

我们要在黑客松期间做出一个可上传到抖音虚拟创作平台、可被用户或评委直接扫码体验的离线 H5 互动作品。

不是做一个功能很多的系统，而是做一个短时间内能理解、能上手、能玩完、玩完愿意分享的互动体验。

## 2. 赛道原意

互动空间的核心是：

- 想到：一个原始创意可以很粗糙。
- 生成：借助 AI / Vibe Coding 快速把创意变成可体验作品。
- 上线：通过抖音虚拟创作平台提交。
- 被玩到：用户或评委能直接体验玩法和价值。

关键理解：

- AI 更适合用于创作过程，不等于作品运行时必须调用 AI。
- 作品运行时必须离线，所以不要设计需要 API、登录、后端、多端联机、实时生成内容的方案。
- 赛道要证明的是“这个创意可以被直接玩到”，不是证明我们搭了复杂系统。

## 3. 最重要的成功标准

优先级从高到低：

1. 玩法与交互好玩，容易上手。
2. 创意有新意或惊喜。
3. 有情绪、故事或共鸣。
4. 视觉风格统一，有吸引力。
5. 玩完有分享动机。

黑客松判断口径：

- 1 分钟内能理解。
- 3 分钟内能上手。
- 一轮体验能闭环。
- 结束后有“再玩一次 / 给朋友看”的理由。

不要追求：

- 功能很多但不好玩。
- 需要解释很久才懂。
- 依赖联网、账号、后端或外部资源。
- 一开始就做复杂框架。

## 4. 评审权重

| 维度 | 权重 | 我们的设计含义 |
| --- | ---: | --- |
| 玩法与交互 | 30% | 第一优先级，必须可玩、顺手、反馈明确 |
| 创意与惊喜 | 20% | 要有一个能被记住的核心点 |
| 情感体验 | 15% | 可以有故事、反差、共鸣、角色感 |
| 视觉表现 | 15% | 风格统一，比堆素材更重要 |
| 传播潜力 | 20% | 结算页、结果表达、再玩动机要设计进去 |

## 5. 平台硬约束

这些是红线，不要和它们对抗。

### 5.1 交付形态

- 只能交付单个 `index.html`，或一个可压缩为 `.zip` 的目录。
- 如果交付 `.zip`，解压后根目录必须直接包含 `index.html`。
- 压缩包不要多包一层目录。
- 包体总大小必须 `<= 8MB`。

正确结构：

```text
demo.zip
├── index.html
├── main.js
├── style.css
└── assets/
```

错误结构：

```text
demo.zip
└── project/
    └── index.html
```

### 5.2 离线限制

运行时必须完全离线：

- 禁止 `fetch()`
- 禁止 `XMLHttpRequest`
- 禁止 `axios`
- 禁止 `WebSocket`
- 禁止 `EventSource`
- 禁止远程 `<script>`
- 禁止 CDN
- 禁止远程图片、字体、音频、样式

如果需要资源，必须放进本地目录，并使用相对路径。

### 5.3 跳转与嵌入限制

禁止：

- `<iframe>`
- `window.location`
- `location.href`
- `window.open()`
- 指向站外的 `<a href="https://...">`

### 5.4 代码安全限制

禁止：

- `eval`
- `new Function`
- 远程代码注入
- 危险 HTML 拼接
- `alert()`
- `confirm()`
- `prompt()`
- `print()`

事件绑定优先使用 `addEventListener`，不要把 `onclick`、`onload`、`ontouchstart` 写在 HTML 属性里。

## 6. 默认技术策略

除非后续玩法明确需要，否则默认这样做：

- 单文件 `index.html` 优先。
- 竖屏优先。
- 轻量 DOM/CSS 优先。
- Canvas 只在玩法明显需要大量绘制、物理或高频动画时使用。
- 默认无音频，除非音效能明显增强体验。
- 不引入大型第三方库。
- 简单图形优先用 CSS、SVG、Canvas 或 Base64。
- 如果用第三方库，必须本地打包，且体积可控。

最低运行目标：

- 手机端可用。
- 无横向滚动。
- 触控操作正常。
- 尽量稳定 `30 FPS` 或更高。
- 出错时有兜底，不白屏。

## 7. UI 与交互规范

作品必须像一个真实可玩的互动体验，而不是一个页面展示。

必须包含：

- 开始态。
- 主玩法态。
- 明确反馈。
- 失败 / 成功 / 结算态。
- 重开或再来一次。

弹层要求：

- 所有提示、确认、暂停、胜负、结算都用作品内部 DOM 或 Canvas 自绘。
- 不使用浏览器原生弹窗。
- 同一时间只允许一个主弹层激活。
- 弹层显示和隐藏用状态或 class 切换。

移动端要求：

- 按钮热区足够大。
- 文字在小屏幕上可读。
- 布局适配安全区、不同 DPR、不同宽高比。
- 不要依赖 hover。

## 8. 数据与存档

如需保存数据，只使用 `localStorage`。

可保存：

- 最高分。
- 最近一次结果。
- 用户设置。

要求：

- `localStorage` key 必须带业务前缀。
- 不保存个人敏感信息。
- 不依赖登录、服务端或外部同步。

示例：

```javascript
localStorage.setItem("hackerson_demo_high_score", String(score));
```

## 9. 内容安全红线

不要做：

- 涉政、涉恐、涉暴、赌博、欺诈、低俗色情、邪教、封建迷信。
- 未成年人危险行为模仿、烟酒等不良引导。
- 未授权 IP、角色、Logo、商标、音乐、字体、图片、肖像、姓名。
- 拜金、炫富、歧视、引战、挑动对立。
- 强制分享、强制关注、站外交易、诱导下载。
- 广告、二维码、外部联系方式、导流信息。

如果创意需要借用知名 IP，要改成原创通用表达。

## 10. 我们的产品策略

### 10.1 选题标准

一个好选题至少满足：

- 一句话能讲清楚。
- 玩法动作少，反馈强。
- 能在 30 秒内进入核心乐趣。
- 一轮在 1 到 3 分钟内结束。
- 结算结果有表达欲。

### 10.2 玩法闭环

每个创意都要先写清楚：

```text
玩家做什么 -> 系统给什么反馈 -> 玩家为什么继续 -> 什么时候结束 -> 结束后为什么愿意分享
```

如果这条链路说不清，先不要写代码。

### 10.3 黑客松范围控制

优先做一个小而完整的作品：

- 核心循环完整。
- 体验不白屏。
- 包体合规。
- 手机上能玩。
- 有可展示的亮点。

暂缓做：

- 账号系统。
- 排行榜服务端。
- 多人联机。
- 复杂关卡编辑器。
- 大量音频 / 大图 / 3D 资产。
- 需要远程模型或联网推理的玩法。

## 11. 3 人团队建议分工

| 角色 | 主要职责 | 交付物 |
| --- | --- | --- |
| 玩法 / PM | 定主题、玩法闭环、评审卖点、验收清单 | 玩法说明、状态流、评分点对齐 |
| 前端实现 | `index.html`、交互逻辑、移动端适配、性能兜底 | 可运行作品、核心逻辑、错误兜底 |
| 视觉 / 测试 / 打包 | 视觉风格、文案、手机测试、包体检查、上传材料 | 封面/描述、测试记录、`demo.zip` |

3 人不是固定岗位，黑客松期间可以交叉，但每天必须有人负责最终合规和打包。

## 12. 推荐项目结构

单文件优先：

```text
index.html
```

多文件只在必要时使用：

```text
/
├── index.html
├── style.css
├── main.js
├── assets/
│   ├── images/
│   ├── audio/
│   └── icons/
└── README.md
```

## 13. 开发流程

### Step 1：归一化需求

先确定：

- 作品名称。
- 玩法类型。
- 核心交互。
- 胜负 / 结算条件。
- 屏幕方向。
- DOM 还是 Canvas。
- 是否需要存档。
- 单文件还是多文件。

### Step 2：合规门禁

写代码前检查：

- 是否触碰内容安全红线。
- 是否需要联网。
- 是否需要外部资源。
- 是否会超 8MB。
- 是否适合手机端触控。

### Step 3：做最小可玩版本

先做：

- 开始。
- 核心玩法。
- 反馈。
- 结算。
- 重开。
- 错误兜底。

后做：

- 动效强化。
- 视觉 polish。
- 分享动机文案。
- 关卡或变体。

### Step 4：手机端验证

至少验证：

- 小屏宽度。
- 竖屏高度。
- 触控点击。
- 无横向滚动。
- 一轮能完成。
- 出错不白屏。

### Step 5：打包上传

上传前生成 `demo.zip`，确保 `index.html` 在 zip 根目录。

## 14. 上传前自检清单

### 14.1 平台合规

- [ ] 根目录存在 `index.html`
- [ ] ZIP 包体 `<= 8MB`
- [ ] 没有 `fetch`
- [ ] 没有 `XMLHttpRequest`
- [ ] 没有 `axios`
- [ ] 没有 `WebSocket`
- [ ] 没有 `EventSource`
- [ ] 没有外部 CDN
- [ ] 没有远程图片、字体、音频或脚本
- [ ] 没有 `<iframe>`
- [ ] 没有外部跳转
- [ ] 没有 `alert()` / `confirm()` / `prompt()` / `print()`
- [ ] 没有 `eval` / `new Function`
- [ ] 所有资源使用相对路径

### 14.2 移动端体验

- [ ] 手机端无横向滚动
- [ ] 触摸操作正常
- [ ] 按钮和文字可读可点
- [ ] 不依赖 hover
- [ ] 有错误兜底，不白屏
- [ ] 动画不卡顿，尽量 `30 FPS+`

### 14.3 玩法体验

- [ ] 主要玩法 1 分钟内能理解
- [ ] 核心互动 3 分钟内能完成
- [ ] 有开始、主玩法、结算、重开
- [ ] 反馈足够明确
- [ ] 玩完有分享动机
- [ ] 作品卖点能一句话讲清楚

## 15. 上传流程

1. 登录抖音虚拟创作平台。
2. 创建作品，填写标题、描述、分类、封面。
3. 上传 `demo.zip`。
4. 生成二维码后，用抖音 APP 扫码体验。
5. 确认体验无误后发布审核。

审核时间参考：约 1 个工作日。

## 16. 当前默认决策

在没有更具体创意前，我们默认采用：

- 交付等级：黑客松可演示版本。
- 形态：离线 H5。
- 方向：竖屏。
- 文件：优先单文件 `index.html`。
- 技术：原生 HTML / CSS / JavaScript。
- 资源：尽量不用外部素材；必须用素材时本地化并压缩。
- 玩法目标：小、完整、有反馈、有结算、有分享理由。

## 17. 后续所有创意先过这 5 个问题

每次提出玩法，先回答：

1. 玩家第一下要做什么？
2. 30 秒内能不能进入核心乐趣？
3. 一轮什么时候结束？
4. 结算结果为什么值得给别人看？
5. 这个玩法有没有违反离线、8MB、无外链、手机触控限制？

如果 5 个问题答不清，先做玩法 spike，不直接堆代码。
